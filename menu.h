#ifndef MENU_H
#define MENU_H

// Enum que mapeia os números para nomes claros
enum OpcoesMenu {
    NOVO_JOGO = 1,
    VER_RANKING,
    SAIR
};

// Declaração da função
int Menu();

#endif // MENU_H
